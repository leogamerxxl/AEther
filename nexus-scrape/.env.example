# ===== AETHER Tier 1 pipeline - SERVER-SIDE ONLY (never commit real values, never frontend)
SUPABASE_URL=https://irmyramqaovmgcktbazy.supabase.co
SUPABASE_SERVICE_ROLE_KEY=
APIFY_API_TOKEN=
APIFY_ACTOR=voyager~booking-scraper
OWM_API_KEY=
ANTHROPIC_API_KEY=
RESEND_API_KEY=
OTB_SHEET_ID=
GOOGLE_SERVICE_ACCOUNT_JSON=
BRIEF_RECIPIENTS=
BRIEF_FROM=
ALERT_EMAIL=
ALERT_FROM=onboarding@resend.dev
OTB_CAPACITY=52
RATES_DAYS=14
MIN_RATE_COVERAGE=10
STAGE_RETRIES=2
RETRY_BACKOFF_S=60
PIPELINE_ENABLED=true
# Crontab (UTC; EEST = UTC+3):
# 30 11 * * *  cd /opt/aether/nexus-scrape && python run_daily.py fx
# 30  1 * * *  cd /opt/aether/nexus-scrape && python run_daily.py rates
# 45  1 * * *  cd /opt/aether/nexus-scrape && python run_daily.py weather
#  0  2 * * *  cd /opt/aether/nexus-scrape && python run_daily.py otb
# 30  2 * * *  cd /opt/aether/nexus-scrape && python run_daily.py validate
#  0  3 * * *  cd /opt/aether/nexus-scrape && python run_daily.py brief
# 45  3 * * *  cd /opt/aether/nexus-scrape && python run_daily.py send
# 55  3 * * *  cd /opt/aether/nexus-scrape && python run_daily.py sentinel